---
- name: Create 10 nginx containers on all servers
  hosts: all
  tasks:
    - name: Create multiple containers
      docker_container:
        name: "container{{item}}"
        image: nginx
      with_sequence: count=10

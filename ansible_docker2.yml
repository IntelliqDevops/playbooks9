---
- name: Working on docker containers
  hosts: all
  tasks:
    - name: Create a tomcat container
      docker_container:
        name: appserver
        image: tomcat
    - name: Stop the tomcat container and delete it
      docker_container:
        name: appserver
        state: "{{item}}"
      with_items:
        - stopped
        - absent
